# ── Hosted (Streamlit Cloud) runtime ──────────────────────────────────────
# Kept lean so the app wakes fast. Heavy scrape/email-only deps (playwright,
# pdfplumber, msal) live in requirements-dev.txt for local use / auto_import.
streamlit>=1.40.0
python-dotenv==1.0.1
psycopg2-binary>=2.9.9
pydantic>=2.0.0
holidays>=0.50
requests>=2.31.0
httpx==0.27.0
beautifulsoup4==4.12.3
pillow>=11.0.0
openpyxl>=3.1.0            # River FOB workbook parse + Excel export
vl-convert-python>=1.7     # Altair→PNG export for seasonal-chart download/copy (Cloud-safe, Rust)
